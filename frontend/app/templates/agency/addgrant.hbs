

<style>

.addgrant {
    background-color: rgba(250,250,250,0.3);
    padding: 30px;
    text-align: center;
}

.addgrant .container {
    max-width: 600px;
}

</style>

<div class='addgrant'>
    <div class='container'>
    <select class="institutionSelect form-control input-lg select" onchange={{action (mut institutionId) value="target.value"}}>
        <option>Select an Institution</option>
        {{#each institutions as |inst|}}
        <option selected={{eq inst institutionChoice}} value={{inst}}>{{inst}}</option>
        {{/each}}
    </select>
    <br>
    <input class='form-control input-lg' value={{grant_number}} onchange={{action (mut grant_number) value='target.value'}} placeholder='Grant Number'>
    <br>
    <button type='button' class='btn btn-primary btn-lg button-pos' {{action 'addGrant' departmentId grant_number grant_pi}}>Add this grant</button>
    </div>
</div>

