{{outlet}}
<div class="form-table">
  {{dynamic-form schema=model.parentModel.grant.questions}}
  <button class="btn btn-primary btn-lg btn-block metaData"  {{action "submit" model}} > Submit Document </button>
</div>