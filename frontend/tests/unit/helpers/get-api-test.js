import { getApi } from 'ostp-files-ember-v2/helpers/get-api';
import { module, test } from 'qunit';

module('Unit | Helper | get api');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getApi([42]);
  assert.ok(result);
});
