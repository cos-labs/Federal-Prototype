<div class="form-table">
  <h1>Connect Grants</h1> Add a grant entry for each grant that was awarded to fund this research
  <hr /> 

  {{#each grants as |grant|}}

  <div class='row'>
    <div class='col-md-10'><h4>{{grant.agency.name}} - {{grant.number}} </h4></div>
    <div class='col-md-2'>
      <button class="btn col-md-5 btn-primary btn-lg btn-block" {{action 'edit' model.grant agencyId fileList model.document}}>Edit</button>
    </div>
  </div>

  {{else}}

  <p>
    No grants currently apply to this document. To connect a grant, Use the drop-down menu to select the agency that awarded the grant, type in your grant number, then press the 'Add a grant' button below.
  </p>

  {{/each}}

  <hr/>

  <div class='row'>

    <div class="dropdownHolder col-md-12">
      <select class="agencySelect input-lg select" onchange={{action (mut agencyId) value="target.value" }}>
        <option>Select an Agency</option>
        {{#each agencies as |agencyChoice|}}
        <option selected={{eq agency agencyChoice}} value={{agencyChoice.id}}>{{agencyChoice.name}}</option>
        {{/each}}
      </select>
    </div>
  </div>
  <div class='row'>
    <!-- grant number for new grant-->
    <div class='col-md-9'>
      {{input type="text" value=grantNumber}}
    </div>
    <div class='col-md-3'>
      <button class="btn btn-success" {{action "addGrant" model.grants agencyId grantNumber}}>Add a grant</button>
    </div>
  </div>
  <button class="btn btn-primary btn-lg btn-block metaData" {{action 'submit' model.grant agencyId fileList model.document}}>Finish</button>
</div>
