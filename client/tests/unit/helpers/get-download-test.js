import { getDownload } from 'ostp-files-ember-v2/helpers/get-download';
import { module, test } from 'qunit';

module('Unit | Helper | get download');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDownload([42]);
  assert.ok(result);
});
